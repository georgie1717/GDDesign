---
---

<div class="lead prose">
  <slot />
</div>

<style>
  .lead { margin-bottom: 28px; }

  .lead :global(p) {
    font-size: var(--t-body-lg);
    line-height: 1.5;
    color: var(--ink);
  }
</style>
